# 系统分析报告
生成时间: 2026-04-07 22:41:01

## system.py
### 初始化逻辑
```python
def __init__(self, model_dir="models", data_dir="data", output_dir="output"):
        self.model_dir = model_dir
```

### 资源管理
```python
model
model
model
```

### 异常处理
```python

```

### 目录创建/权限
```python
os.makedirs
os.makedirs
```

## inference.py
### 模型推理流程
```python
def detect(self, text, url, use_cache=True):
        """检测钓鱼文本"""
```

### 多模态特征融合逻辑
```python
Multimodal
url_features
network_features
```

### 置信度计算
```python
prob
confidence
```

### 效率优化
```python
cache
cache
cache
```

## data_preprocessing.py
### 文本清洗
```python
Preprocess
text
```

### URL特征提取规则
```python
URLFeature
extract_features(self, url
extract_domain(url
```

### 网络行为特征采集逻辑
```python
extractor = Network
network_features = df['url'].apply(lambda x: self.network_extractor.extract_feature
```

### 数据归一化/标准化
```python

```

## model_training.py
### BERT-TextCNN混合模型定义
```python
class BERTTextCNN(nn.Module):
    
```

### 损失函数设计
```python
Loss
criterion
```

### 训练策略
```python
optimizer
optimizer
optimizer
```

### 验证集划分
```python
train_test_split
train_test_split
```

## incremental_training.py
### 增量数据融合方式
```python
incremental_data_path = f"{self.data_dir}/incremental/incremental_data
incremental_data
```

### 模型参数更新策略
```python
update
```

### 过拟合防控
```python

```

### 评估指标完整性
```python
metric
accuracy
recall
```

## scenario_processor.py
### 不同场景的差异化处理逻辑
```python
process_sms
process_email
process_link
```

### 场景特征权重设计
```python

```

## visualization.py
### 可视化图表的准确性
```python
plot
visualize
```

### 特征重要性计算逻辑
```python
feature_importance
```

### 攻击路径分析的合理性
```python
attack_path
attack_path": self.generate_attack_path
```

## 辅助文件分析
### requirements.txt
文件不存在

### Dockerfile
```
# 使用Python 3.12作为基础镜像
FROM python:3.12-slim

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    curl \
    git \
    && rm -rf /var/lib/apt/lists/*

# 复制requirements.txt文件
COPY api/requirements.txt /app/requirements.txt

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制项目文件
COPY . /app

# 创建必要的目录
RUN mkdir -p /app/data/synthetic \
    && mkdir -p /app/data/real \
    && mkdir -p /app/data/multimodal \
 ...
```

### deploy.sh
```
#!/bin/bash

# 部署脚本

echo "===== 多模态钓鱼智能识别与预警系统部署 ====="

# 检查Docker是否安装
if ! command -v docker &> /dev/null; then
    echo "错误: Docker未安装，请先安装Docker"
    exit 1
fi

# 检查Docker Compose是否安装
if ! command -v docker-compose &> /dev/null; then
    echo "错误: Docker Compose未安装，请先安装Docker Compose"
    exit 1
fi

# 构建Docker镜像
echo "正在构建Docker镜像..."
docker-compose build

# 启动容器
echo "正在启动容器..."
docker-compose up -d

# 等待服务启动
echo "等待服务启动..."
sleep 10

# 验证服务是否正常运行
echo "验证服务是否正常运行..."
response=$(curl -s h...
```

